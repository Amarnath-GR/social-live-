name: social_live_flutter
description: "Social Live MVP - Flutter Frontend"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  video_player: ^2.8.1
  camera: ^0.10.5+9
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  chewie: ^1.7.5
  video_thumbnail: ^0.5.3
  share_plus: ^10.1.3
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true